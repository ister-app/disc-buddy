import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (WidgetTester tester) async {
    // Placeholder — real integration tests require a Linux optical drive.
    expect(true, isTrue);
  });
}
