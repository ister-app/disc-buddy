name: disc_buddy_cli
description: Disc Buddy CLI — rip Audio CDs, DVDs and Blu-rays from the command line.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  disc_buddy:
    path: ../disc_buddy_lib
  args: ^2.6.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
